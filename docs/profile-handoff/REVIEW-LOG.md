# Review log — pipeline audit trail

This packet was produced by a three-stage pipeline: draft → adversarial
review (27 critique items, C1–C27, each verified against
`eliterank-co/eliterank-app` @ `c2f45dd`) → merge. This file records what the
merge did with every critique item, and the verification that ran on the
final packet. New factual claims introduced by fixes were re-verified against
source before being written.

**Dispositions: 20 applied · 7 applied-with-modification · 0 rejected.**

| Item | Severity | Disposition | What was done |
| --- | --- | --- | --- |
| C1 | Blocker | applied | R3 recast as a Craft/discoverability finding — the avatar menu (`menu.ts:182-194`, re-verified: links `/me/votes`, `/me/watching`, `/me/history`, `/me/transactions`, `/me/settings` via `layout.tsx:16` → `nav.tsx:43` → `header-widgets.tsx:20,44`) reaches every member destination in both experiences, so "only UI path" was false. Cascades fixed: R19 text, V2/V4/V7 view docs, old→new map G5 row; AC-V2-03 is now a reachability **guard** (`T-AC-V2-03`, plain test against the real `Profile menu` button / `role=menu` markup, desktop + 375px) plus a recorded discoverability decision. R-numbers unchanged. |
| C2 | Major | applied-with-modification | The phantom `T-AC-V2-06` now exists: `tests/unit/dead-components.test.ts` (fs census — @deprecated check + single-`ProfileHero`-exporter check). Modification: ships `describe.skip` (post-fix acceptance) since its assertions fail at `c2f45dd` by design, keeping CI green; traceability and checklist say so. |
| C3 | Blocker | applied | (a) `social-handle.test.ts` carries a DOES-NOT-COMPILE banner (needs the R5 extraction of `extractSocialHandle` to `src/lib/social-handle.ts`); every "passes today" claim for AC-V3-02 removed — it is now "(unit, contract-first)" everywhere and excluded from the expected-green guard set. (b) `track-record-labels.test.ts` carries the same banner (`deriveTier`/`derivePlacementLabel` module-private; `deriveHostedDates` nonexistent). (c) light-theme units restructured: the parity check is the only active test; the v2-declaration, token-census, color-scheme, and hardcoded-census assertions are `skip`-marked post-fix acceptance with reasons — the suite is green day one without lying. tests/README states readiness per file; no doc claims an executed result anywhere ("expected green at `c2f45dd`" is defined as source-verified, unrun). |
| C4 | Major | applied | Totals regenerated by script from the final matrix (output below): 49 ACs, 41 carrying a test, 8 manual-only (5 decisions + `AC-V10-04/06/07`), 5 with both halves, findings 13/3/5/3. The old "36 automated / 4 manual-half / 7 decision-only" numbers are gone. |
| C5 | Major | applied | AC-V2-07 rewritten: Watching count + bonus checklist + Edit absent in Preview Mode **and the owner strip present** (it hosts Exit Preview, `profile-page-view.tsx:75-100` — re-verified: `isOwner && !isPreviewAsVisitor` gates watchingCount/Edit/bonus, the strip gates on `isOwner` alone); strip absence on `/p/[voterId]` is checked by `T-AC-V8-08`. `T-AC-V2-07` asserts the strip and Exit Preview explicitly. |
| C6 | Major | applied-with-modification | `_fixtures.ts` no longer inlines any cookie: `selectExperience(context, baseURL, exp)` (signature now matches the docs) delegates the profile branch to the app-owned `enableUiSurfaces(context, baseURL, surfaces)`; the dashboard branch throws with a named app-owned TODO (`DASHBOARD_TODO`) and every dashboard-experience spec skips on `CAN_SELECT_DASHBOARD` until the app team ships an off-selection helper. `BASE_URL` default corrected to `http://localhost:3010` (app `playwright.config.ts:62-63`, re-verified). Modification: T-AC-V6-02 was moved to profile-experience selection so that guard stays runnable (its loader is experience-independent). |
| C7 | Major | applied | `T-AC-V3-04` uses the new `MEMBER_WITH_VIDEO` fixture and asserts the video exists (`not.toHaveCount(0)`) before asserting the poster — the `if (count)` escape is gone. |
| C8 | Major | applied-with-modification | `T-AC-V6-02` now asserts the seeded rows render: `Saved markets (N)` against `MIXEDCASE_WATCH_COUNT` (required env ≥1; skips without it). Modification: the count is read from the panel title that exists today (`me/watching/page.tsx:42`, re-verified) rather than a not-yet-existing `watching-row` testid, so the guard can be green before any instrumentation lands. |
| C9 | Major | applied-with-modification | AC-V1-05 restated: non-host label set ⊆ host label set; host tiles additive, never renamed (now consistent with RQ-V1-2). Modification: 05a/05b merged into one `T-AC-V1-05` that opens both fixtures in one test and does the real subset comparison — no cross-test annotation passing. |
| C10 | Major | applied | `T-AC-V8-04` gains the positive half: `TARGET_VOTER_ID_WINNER` fixture, WINNER tab must remain for a crowned member. A delete-all-tabs fix now fails. |
| C11 | Major | applied | `T-AC-V3-06` is either-arm on the new `MEMBER_WITH_TAGS` fixture: editor present → visible + offers the taxonomy; editor absent → the interests card must be gone from `/me` even though tags exist (which fails today — R17 — so the fixme is honest and neither arm is vacuous). |
| C12 | Major | applied | Instrumentation inventory regenerated mechanically from the final specs (`grep -ho 'data-testid="[^"]*"'`, output below) into REQ-19 and tests/README: `bonus-task-row`(+`data-completed`), `fan-toggle`, `hero-count-label`, `history-entry`(+`data-role`), `interests-editor`, `member-tier`(+`data-source`), `stat-label`, `watching-row`. `member-nav` and `track-record-entry` are gone; the docs state `timeline` exists but is selected by no spec. |
| C13 | Major | applied | REQ-12 scoped to the voter profile editor (`ProfileEdit`, one component behind `/me/profile` and the crossover `/profile`); the host org editor (`updateOrganizationSocials`, `src/app/(public)/host/[orgSlug]/settings/social-actions.ts` — path re-verified) named explicitly out of scope. |
| C14 | Minor | applied | `track-record.ts:210`, `share-card-modal.tsx:38`, `page-v3.tsx:46` — all three re-verified against source and corrected in findings + test comments. |
| C15 | Minor | applied | AC-V10-04 contrast floors re-attributed to `DESIGN-PROMPT.md:171` and `CLAUDE-DESIGN-PROMPT.md:216` (re-verified by grep); INVARIANTS item 30 cited only for the gold split. |
| C16 | Minor | applied | V10 token item 4 restated: 11 of 16 `--legacy-*` vars inherit via `color-mix`/`var()`; 3 are hardcoded `oklch()` tier literals (globals.css:87-89, re-verified) needing their own light values or classification; 2 are motion curves. |
| C17 | Minor | applied | `T-AC-V10-02b` asserts only the resolved `color-scheme` diverging from the emulated system preference; `data-theme` demoted to an example in comments and the view doc. |
| C18 | Minor | applied | `T-AC-V10-03` now ships the scripted census: extracts `var(--…)` consumption across `src/`, parses base vs light-scoped definitions in `globals.css`, and asserts every consumed color-bearing base token has a light definition (with a documented carve-out for pure primitive-derived values). Skip-marked post-fix. |
| C19 | Minor | applied-with-modification | `T-AC-V8-05` no longer greps for the literal anywhere: it asserts no literal default on the `competitionTitle` parameter (regex on the fallback expression — renamed literals still fail, innocent comments don't). Modification: moved to its own runnable file (`share-card-fallback.test.ts`, skip-marked post-fix) so it is not trapped inside the non-compiling contract-first file. |
| C20 | Minor | applied | No `T-AC-V10-04` exists anywhere in the packet; AC-V10-04 is Manual, with the contrast script named as an artifact of the palette work, explicitly "not shipped in this packet". |
| C21 | Minor | applied-with-modification | Matrix now names `T-AC-V10-02a/b/c` matching the spec. Modification: rather than keeping the V1-05a/b split convention, V1-05 was collapsed to a single `T-AC-V1-05` (see C9) — the convention is now: the matrix names exactly the test IDs that exist. |
| C22 | Minor | applied | Old→new map's "New at `c2f45dd`" row lists only R10 and R23 — every other R appears exactly once, on its ancestor's row (R2←G3, R3←G5, R4←S4, R9←S2, etc.). |
| C23 | Minor | applied | README states the real split: thirteen prior findings improved — six resolved, two mostly, five partially (matches the 13-row resolved table). |
| C24 | Minor | applied-with-modification | Exp column aligned to the specs: AC-V3-01/03/04/05/06 → profile, AC-V7-01/03/04/05 → dashboard, AC-V9-01 → profile. Modification beyond the item: AC-V6-02 → profile, because the spec itself changed (see C6/C8). |
| C25 | Minor | applied | The gated V7 deletion describe now sets `storageState: FIXTURES.MEMBER_HOST` (with the live-competition requirement documented in the fixture table), so T-AC-V7-03 no longer runs unauthenticated. |
| C26 | Minor | applied | `T-AC-V1-01` allows a derived tier: either a `member-tier` element carrying `data-source` (derivation named), or no tier element and no literals. A legitimate derived-Gold fix passes; today's hardcoded render still fails. |
| C27 | Minor | applied | assets/README names two grammars explicitly: `IMG-<view>-<letter>[-<state>].png` and `IMG-V10-<view>-light.png`; the table row now matches. |

## Verification on the final packet

**1. AC reconciliation (scripted).** 49 AC definitions in `views/*.md`; 49
matrix rows in `traceability.md`; set difference both directions: empty.
42 distinct `T-AC-*` ids referenced by the matrix (a/b/c expanded), 42 found
in `tests/**`; missing-from-files: none; not-in-matrix: none. Scripted
totals — 41 rows carry a test; manual-only: `AC-V2-05`, `AC-V3-07`,
`AC-V5-03`, `AC-V7-02`, `AC-V7-06`, `AC-V10-04`, `AC-V10-06`, `AC-V10-07`;
both-halves: `AC-V2-03`, `AC-V3-03`, `AC-V3-05`, `AC-V8-07`, `AC-V10-08`;
findings 24 = 13 Defect / 3 Policy / 5 Product / 3 Craft — all equal to the
published totals.

**2. Link/anchor audit (scripted, GitHub slug rules).** 138 relative links
checked across all `.md`; 0 broken (the only failure during the run was this
file not existing yet).

**3. TS parse check.** `tsc --noEmit --skipLibCheck` over all 16 `.ts`
files: zero syntax errors. Remaining diagnostics are exclusively
environment noise outside the app checkout — TS2307 (missing
`@playwright/test`/`vitest`/`@/lib/*` modules), TS2591 (`process`/node
types), and TS7031/TS7006 implicit-any cascades from the untyped Playwright
import. Brace/paren balance clean on all files (the one counter flag, in
`light-theme-tokens.test.ts`, is braces inside regex character classes —
tsc parses the file without error).

**4. Honesty-string grep.** `passes today`: 0 occurrences. `green today`:
0 occurrences. `runnable now`: 1 occurrence — the banner in
`light-theme-tokens.test.ts` labelling its parity check "RUNNABLE NOW",
which is true: that file compiles and executes at `c2f45dd` with no app
imports, and its only active test guards the brand-contract parity verified
byte-identical in this audit (it skips when sibling checkouts are absent).
All "expected green at `c2f45dd`" phrases are defined in-packet as
"written from source, unrun" — no executed-result claim exists anywhere:
this audit ran nothing.
