Screenshots are captured, not generated. See ../README.md for the capture
spec: filenames, flag state per shot, and what each one is evidence for.

Redact member email addresses before committing — this repository is public.
